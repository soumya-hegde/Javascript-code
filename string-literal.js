const source = "bangalore",
  destination = "mysore",
  distance = 140,
  time = "2 hours, 30 mins";
console.log(
  "The distance between " + source + " and " + destination + " is " + distance + " kms " + "and the time taken to travel is " + time + "."
);

console.log(
  `The distance between ${source} and ${destination} is ${distance} kms and the time taken to travel is ${time}.`
);

